# Copie este arquivo para .env.local e ajuste conforme necessário.

# Quando "true", o app usa os dados mockados em src/mocks em vez de chamar o backend real.
# Útil enquanto os endpoints da API ainda não estão finalizados.
VITE_USE_MOCKS=true
